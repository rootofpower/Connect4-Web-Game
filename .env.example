POSTGRES_DB=gamestudiodb
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_URL=jdbc:postgresql://db:5432/gamestudiodb
JWT_SECRET=
JWT_EXPIRATION=3600000
# 'update' lets Hibernate create/upgrade the schema on a fresh clone (database/init.sql is not auto-loaded).
# Switch to 'validate' only if you load init.sql via database/Dockerfile's COPY line.
SPRING_JPA_HIBERNATE_DDL_AUTO=update
VITE_API_URL=
